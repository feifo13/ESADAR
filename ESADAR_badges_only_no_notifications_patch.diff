--- /mnt/data/esadar_iter2/frontend/src/main.jsx	2026-05-14 15:35:20.963858569 +0000
+++ /mnt/data/esadar_badges_only_work/frontend/src/main.jsx	2026-05-14 15:45:36.231013719 +0000
@@ -7,7 +7,6 @@
 import { ThemeProvider } from './contexts/ThemeContext.jsx';
 import { AuthProvider } from './contexts/AuthContext.jsx';
 import { CartProvider } from './contexts/CartContext.jsx';
-import { ActionNotificationsProvider } from './contexts/ActionNotificationsContext.jsx';
 import { LookupsProvider } from './contexts/LookupsContext.jsx';
 import { SiteSeoProvider } from './contexts/SiteSeoContext.jsx';
 import { WishlistProvider } from './contexts/WishlistContext.jsx';
@@ -33,11 +32,9 @@
             <SiteSeoProvider>
               <WishlistProvider>
                 <CartProvider>
-                  <ActionNotificationsProvider>
-                    <ErrorBoundary>
-                      <App />
-                    </ErrorBoundary>
-                  </ActionNotificationsProvider>
+                  <ErrorBoundary>
+                    <App />
+                  </ErrorBoundary>
                 </CartProvider>
               </WishlistProvider>
             </SiteSeoProvider>
--- /mnt/data/esadar_iter2/frontend/src/components/Header.jsx	2026-05-14 15:36:14.792249360 +0000
+++ /mnt/data/esadar_badges_only_work/frontend/src/components/Header.jsx	2026-05-14 15:45:36.248595767 +0000
@@ -1,725 +1,665 @@
-import { Link, NavLink, useLocation, useNavigate } from "react-router-dom";
-import { useEffect, useRef, useState } from "react";
-import { motion, useReducedMotion } from "motion/react";
-import { useAuth } from "../contexts/AuthContext.jsx";
-import { useCart } from "../contexts/CartContext.jsx";
-import { useActionNotifications } from "../contexts/ActionNotificationsContext.jsx";
-import MobileMotionMenu from "./MobileMotionMenu.jsx";
-import { useMobileMenu } from "../contexts/MobileMenuContext.jsx";
-import esadarWordmark from "../assets/esadar-wordmark.webp";
-
-function findVisibleTargetRect(...refs) {
-  for (const ref of refs) {
-    const element = ref.current;
-    if (!element) continue;
-    const rect = element.getBoundingClientRect();
-    if (rect.width > 0 && rect.height > 0) return rect;
-  }
-  return null;
-}
-
-function HeaderIcon({ children }) {
-  return (
-    <span className="header-icon-button__icon" aria-hidden="true">
-      {children}
-    </span>
-  );
-}
-
-function AccountIcon() {
-  return (
-    <svg
-      viewBox="0 0 24 24"
-      fill="none"
-      stroke="currentColor"
-      strokeWidth="1.9"
-      strokeLinecap="round"
-      strokeLinejoin="round"
-    >
-      <path d="M20 21a8 8 0 0 0-16 0" />
-      <circle cx="12" cy="8" r="4" />
-    </svg>
-  );
-}
-
-function HeartIcon() {
-  return (
-    <svg
-      viewBox="0 0 24 24"
-      fill="none"
-      stroke="currentColor"
-      strokeWidth="1.9"
-      strokeLinecap="round"
-      strokeLinejoin="round"
-    >
-      <path d="M20.8 4.6a5.5 5.5 0 0 0-7.8 0L12 5.6l-1-1a5.5 5.5 0 1 0-7.8 7.8l1 1L12 21l7.8-7.6 1-1a5.5 5.5 0 0 0 0-7.8z" />
-    </svg>
-  );
-}
-
-function AdminIcon() {
-  return (
-    <svg
-      viewBox="0 0 24 24"
-      fill="none"
-      stroke="currentColor"
-      strokeWidth="1.9"
-      strokeLinecap="round"
-      strokeLinejoin="round"
-    >
-      <path d="M12 3l7 4v5c0 5-3.4 7.7-7 9-3.6-1.3-7-4-7-9V7l7-4z" />
-      <path d="M9.5 12.5l1.6 1.6 3.4-3.7" />
-    </svg>
-  );
-}
-
-function LoginIcon() {
-  return (
-    <svg
-      viewBox="0 0 24 24"
-      fill="none"
-      stroke="currentColor"
-      strokeWidth="1.9"
-      strokeLinecap="round"
-      strokeLinejoin="round"
-    >
-      <path d="M15 3h4a2 2 0 0 1 2 2v14a2 2 0 0 1-2 2h-4" />
-      <path d="M10 17l5-5-5-5" />
-      <path d="M15 12H3" />
-    </svg>
-  );
-}
-
-function LogoutIcon() {
-  return (
-    <svg
-      viewBox="0 0 24 24"
-      fill="none"
-      stroke="currentColor"
-      strokeWidth="1.9"
-      strokeLinecap="round"
-      strokeLinejoin="round"
-    >
-      <path d="M9 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h4" />
-      <path d="M16 17l5-5-5-5" />
-      <path d="M21 12H9" />
-    </svg>
-  );
-}
-
-function SearchIcon() {
-  return (
-    <svg
-      viewBox="0 0 24 24"
-      fill="none"
-      stroke="currentColor"
-      strokeWidth="1.9"
-      strokeLinecap="round"
-      strokeLinejoin="round"
-    >
-      <circle cx="11" cy="11" r="7" />
-      <path d="M20 20l-3.5-3.5" />
-    </svg>
-  );
-}
-
-function FilterIcon() {
-  return (
-    <svg
-      viewBox="0 0 24 24"
-      fill="none"
-      stroke="currentColor"
-      strokeWidth="1.9"
-      strokeLinecap="round"
-      strokeLinejoin="round"
-    >
-      <path d="M4 5h16" />
-      <path d="M7 12h10" />
-      <path d="M10 19h4" />
-    </svg>
-  );
-}
-
-function SortIcon() {
-  return (
-    <svg
-      viewBox="0 0 24 24"
-      fill="none"
-      stroke="currentColor"
-      strokeWidth="1.9"
-      strokeLinecap="round"
-      strokeLinejoin="round"
-    >
-      <path d="M7 4v16" />
-      <path d="M4 7l3-3 3 3" />
-      <path d="M17 20V4" />
-      <path d="M14 17l3 3 3-3" />
-    </svg>
-  );
-}
-
-function MenuGlyph() {
-  return (
-    <svg
-      viewBox="0 0 24 24"
-      aria-hidden="true"
-      className="menu-glyph"
-      fill="none"
-      stroke="currentColor"
-      strokeWidth="2"
-      strokeLinecap="square"
-      strokeLinejoin="round"
-    >
-      <path d="M5 7h14M5 12h14M5 17h14" />
-    </svg>
-  );
-}
-
-export default function Header({ hideBrand = false }) {
-  const navigate = useNavigate();
-  const location = useLocation();
-  const { user, logout, isAuthenticated } = useAuth();
-  const { cartCount, cartFx } = useCart();
-  const { summary: actionNotifications } = useActionNotifications();
-  const {
-    catalogFiltersContent,
-    catalogSortContent,
-    catalogFiltersCount,
-    catalogSortActive,
-    clearCatalogFilters,
-    clearCatalogSort,
-  } = useMobileMenu();
-  const shouldReduceMotion = useReducedMotion();
-  const [cartPulse, setCartPulse] = useState(false);
-  const [flyFx, setFlyFx] = useState(null);
-  const [mobileMenuOpen, setMobileMenuOpen] = useState(false);
-  const [mobileSearch, setMobileSearch] = useState("");
-  const desktopCartButtonRef = useRef(null);
-  const mobileCartButtonRef = useRef(null);
-  const mobileMenuId = "esadar-mobile-menu";
-
-  useEffect(() => {
-    setMobileMenuOpen(false);
-  }, [location.pathname]);
-
-  useEffect(() => {
-    if (!cartFx?.tick) return undefined;
-
-    setCartPulse(true);
-    const timeoutId = window.setTimeout(() => setCartPulse(false), 900);
-
-    const sourceRect = cartFx.sourceRect;
-    const targetRect = findVisibleTargetRect(
-      mobileCartButtonRef,
-      desktopCartButtonRef,
-    );
-
-    if (sourceRect && targetRect) {
-      const startX = sourceRect.left + sourceRect.width / 2;
-      const startY = sourceRect.top + sourceRect.height / 2;
-      const endX = targetRect.left + targetRect.width / 2;
-      const endY = targetRect.top + targetRect.height / 2;
-      const deltaX = endX - startX;
-      const deltaY = endY - startY;
-      const curveLift = Math.max(
-        48,
-        Math.min(160, Math.abs(deltaY) * 0.55 + 72),
-      );
-
-      setFlyFx({
-        id: cartFx.tick,
-        style: {
-          "--cart-fly-start-x": `${startX}px`,
-          "--cart-fly-start-y": `${startY}px`,
-          "--cart-fly-dx": `${deltaX}px`,
-          "--cart-fly-dy": `${deltaY}px`,
-          "--cart-fly-lift": `${curveLift}px`,
-        },
-      });
-
-      const flyTimer = window.setTimeout(() => setFlyFx(null), 960);
-      return () => {
-        window.clearTimeout(timeoutId);
-        window.clearTimeout(flyTimer);
-      };
-    }
-
-    return () => window.clearTimeout(timeoutId);
-  }, [cartFx?.sourceRect, cartFx?.tick]);
-
-  const isAdmin = user?.roles?.some((role) =>
-    ["SUPER_ADMIN", "ADMIN", "OPERATOR"].includes(role),
-  );
-  const canManageUsers = user?.roles?.some((role) =>
-    ["SUPER_ADMIN", "ADMIN"].includes(role),
-  );
-  const isCheckoutView = location.pathname.startsWith("/checkout");
-  const isAccountView = location.pathname.startsWith("/cuenta");
-  const isAdminView = location.pathname.startsWith("/admin");
-
-  function openCart() {
-    navigate("/checkout/resumen", {
-      state: { replayIntro: true, replayIntroReason: "cart" },
-    });
-  }
-
-  function handleMobileSearchSubmit(event) {
-    event.preventDefault();
-    const search = mobileSearch.trim();
-    const path = search
-      ? `/articles?search=${encodeURIComponent(search)}`
-      : "/articles";
-    navigate(path, {
-      state: { scrollToCatalog: true, source: "header-search" },
-    });
-    setMobileMenuOpen(false);
-  }
-
-  function renderCartButton(buttonRef, extraClassName = "") {
-    return (
-      <button
-        ref={buttonRef}
-        type="button"
-        className={[
-          "ghost-button",
-          "cart-button",
-          "cart-button--icon-only",
-          "header-icon-button",
-          cartPulse ? "cart-button--pulse" : "",
-          extraClassName,
-        ]
-          .filter(Boolean)
-          .join(" ")}
-        onClick={openCart}
-        aria-label="Carrito"
-        title="Carrito"
-      >
-        <HeaderIcon>
-          <svg
-            className="cart-button__icon"
-            viewBox="0 0 24 24"
-            fill="none"
-            stroke="currentColor"
-            strokeWidth="2"
-            strokeLinecap="round"
-            strokeLinejoin="round"
-          >
-            <circle cx="9" cy="20" r="1.6" />
-            <circle cx="18" cy="20" r="1.6" />
-            <path d="M3 4h2.2l1.9 9.2a1 1 0 0 0 1 .8h8.8a1 1 0 0 0 1-.8L20 7H7.1" />
-          </svg>
-        </HeaderIcon>
-        <span className={`badge${cartPulse ? " badge--pulse" : ""}`}>
-          {cartCount}
-        </span>
-      </button>
-    );
-  }
-
-  function renderNotificationBadge(count, extraClassName = "") {
-    const safeCount = Number(count || 0);
-    if (!safeCount) return null;
-
-    return (
-      <span
-        className={["notification-badge", extraClassName].filter(Boolean).join(" ")}
-        aria-label={`${safeCount} notificacion${safeCount === 1 ? "" : "es"}`}
-      >
-        {safeCount > 99 ? "99+" : safeCount}
-      </span>
-    );
-  }
-
-  function renderMenuLabel(label, count) {
-    return (
-      <span className="menu-label-with-badge">
-        <span>{label}</span>
-        {renderNotificationBadge(count, "notification-badge--menu")}
-      </span>
-    );
-  }
-
-  function renderIconNavLink(to, label, icon, extraClassName = "", badgeCount = 0) {
-    return (
-      <NavLink
-        to={to}
-        className={({ isActive }) =>
-          [
-            "ghost-button",
-            "header-icon-button",
-            isActive ? "is-active" : "",
-            extraClassName,
-          ]
-            .filter(Boolean)
-            .join(" ")
-        }
-        aria-label={label}
-        title={label}
-      >
-        <HeaderIcon>{icon}</HeaderIcon>
-        <span className="sr-only">{label}</span>
-        {renderNotificationBadge(badgeCount)}
-      </NavLink>
-    );
-  }
-
-  function renderIconButton(onClick, label, icon) {
-    return (
-      <button
-        type="button"
-        className="ghost-button header-icon-button"
-        onClick={onClick}
-        aria-label={label}
-        title={label}
-      >
-        <HeaderIcon>{icon}</HeaderIcon>
-        <span className="sr-only">{label}</span>
-      </button>
-    );
-  }
-
-  const accountMenuChildren = isAuthenticated
-    ? [
-        {
-          key: "account-orders",
-          label: renderMenuLabel("Mis ordenes", actionNotifications.account.orders),
-          to: "/cuenta/ordenes",
-          badgeCount: actionNotifications.account.orders,
-        },
-        {
-          key: "account-offers",
-          label: renderMenuLabel("Ofertas", actionNotifications.account.offers),
-          to: "/cuenta/offers",
-          badgeCount: actionNotifications.account.offers,
-        },
-        { key: "account-saved", label: "Guardados", to: "/cuenta/guardados" },
-        { key: "account-profile", label: "Perfil", to: "/cuenta/perfil" },
-        // { key: "account-alerts", label: "Alertas", to: "/cuenta/alertas" },
-      ]
-    : [
-        { key: "account-login", label: "Ingresar", to: "/login" },
-        { key: "account-register", label: "Crear cuenta", to: "/register" },
-        { key: "account-saved", label: "Guardados", to: "/cuenta/guardados" },
-      ];
-
-  function handleLogout() {
-    logout();
-    setMobileMenuOpen(false);
-    navigate("/", { replace: true });
-    window.scrollTo({ top: 0, behavior: "auto" });
-  }
-
-  const adminMenuChildren = [
-    { key: "admin-articles", label: "Articulos", to: "/admin/articles" },
-    {
-      key: "admin-article-new",
-      label: "Nuevo articulo",
-      to: "/admin/articles/new",
-    },
-    // {
-    //   key: "admin-article-bulk",
-    //   label: "Crear multiples",
-    //   to: "/admin/articles/bulk-create",
-    // },
-    {
-      key: "admin-orders",
-      label: renderMenuLabel("Ordenes", actionNotifications.admin.orders),
-      to: "/admin/orders",
-      badgeCount: actionNotifications.admin.orders,
-    },
-    {
-      key: "admin-offers",
-      label: renderMenuLabel("Ofertas", actionNotifications.admin.offers),
-      to: "/admin/offers",
-      badgeCount: actionNotifications.admin.offers,
-    },
-    {
-      key: "admin-contacts",
-      label: "Contactos",
-      to: "/admin/contact-messages",
-    },
-    { key: "admin-leads", label: "Leads", to: "/admin/leads" },
-    { key: "admin-wishlists", label: "Wishlists", to: "/admin/wishlists" },
-    ...(canManageUsers
-      ? [
-          { key: "admin-users", label: "Usuarios", to: "/admin/users" },
-          { key: "admin-collecting", label: "Cobros", to: "/admin/collecting" },
-        ]
-      : []),
-    { key: "admin-shipping", label: "Envios", to: "/admin/shipping" },
-    { key: "admin-statistics", label: "Estadisticas", to: "/admin/statistics" },
-    { key: "admin-audit", label: "Auditoria", to: "/admin/audit" },
-  ];
-
-  const mobileMenuItems = [
-    // { key: "home", label: "Inicio", to: "/" },
-    {
-      key: "account",
-      label: renderMenuLabel("Mi cuenta", actionNotifications.account.total),
-      kind: "group",
-      badgeCount: actionNotifications.account.total,
-      children: accountMenuChildren,
-    },
-    ...(isAdmin
-      ? [
-          {
-            key: "admin",
-            label: renderMenuLabel("Admin", actionNotifications.admin.total),
-            kind: "group",
-            badgeCount: actionNotifications.admin.total,
-            children: adminMenuChildren,
-          },
-        ]
-      : []),
-    ...(isAuthenticated
-      ? [
-          {
-            key: "logout",
-            label: "Salir",
-            kind: "button",
-            onClick: handleLogout,
-          },
-        ]
-      : []),
-  ];
-
-  function renderMenuButton() {
-    return (
-      <span className="header-mobile-menu-wrap">
-        <motion.button
-          type="button"
-          className="ghost-button header-actions-mobile__menu"
-          aria-controls={mobileMenuId}
-          aria-expanded={mobileMenuOpen}
-          aria-label={mobileMenuOpen ? "Cerrar menu" : "Abrir menu"}
-          onClick={() => setMobileMenuOpen((current) => !current)}
-          initial={false}
-          whileTap={shouldReduceMotion ? undefined : { scale: 0.95 }}
-        >
-          <motion.span
-            animate={
-              mobileMenuOpen
-                ? { rotate: -3, scale: 0.95 }
-                : { rotate: 0, scale: 1 }
-            }
-            transition={
-              shouldReduceMotion ? { duration: 0 } : { duration: 0.12 }
-            }
-          >
-            <MenuGlyph />
-          </motion.span>
-          {renderNotificationBadge(
-            actionNotifications.total,
-            "notification-badge--mobile-menu",
-          )}
-        </motion.button>
-        <span
-          className="mobile-catalog-indicators"
-          aria-hidden={
-            catalogFiltersCount > 0 || catalogSortActive ? undefined : "true"
-          }
-        >
-          {catalogFiltersCount > 0 ? (
-            <button
-              type="button"
-              className="mobile-catalog-indicator mobile-catalog-indicator--filters"
-              onClick={(event) => {
-                event.stopPropagation();
-                clearCatalogFilters?.();
-              }}
-              aria-label={`Limpiar ${catalogFiltersCount} filtros activos`}
-              title="Limpiar filtros"
-            >
-              <FilterIcon />
-              <span>{catalogFiltersCount}</span>
-            </button>
-          ) : null}
-          {catalogSortActive ? (
-            <button
-              type="button"
-              className="mobile-catalog-indicator mobile-catalog-indicator--sort"
-              onClick={(event) => {
-                event.stopPropagation();
-                clearCatalogSort?.();
-              }}
-              aria-label="Restablecer ordenamiento"
-              title="Restablecer orden"
-            >
-              <SortIcon />
-            </button>
-          ) : null}
-        </span>
-      </span>
-    );
-  }
-
-  const mobileSearchContent = (
-    <form className="mobile-menu-search" onSubmit={handleMobileSearchSubmit}>
-      <label className="sr-only" htmlFor="mobile-menu-search-input">
-        Buscar prendas
-      </label>
-      <div className="mobile-menu-search__field">
-        <SearchIcon />
-        <input
-          id="mobile-menu-search-input"
-          className="input"
-          type="search"
-          value={mobileSearch}
-          onChange={(event) => setMobileSearch(event.target.value)}
-          placeholder="Buscar por titulo, marca o categoria"
-        />
-      </div>
-      <button
-        type="submit"
-        className="button button-secondary mobile-menu-search__submit"
-      >
-        Buscar
-      </button>
-    </form>
-  );
-
-  const desktopSearchContent = (
-    <form
-      className="header-search-desktop"
-      onSubmit={handleMobileSearchSubmit}
-      role="search"
-    >
-      <label className="sr-only" htmlFor="site-header-search-input">
-        Buscar prendas
-      </label>
-      <div className="header-search-desktop__field">
-        <SearchIcon />
-        <input
-          id="site-header-search-input"
-          className="input"
-          type="search"
-          value={mobileSearch}
-          onChange={(event) => setMobileSearch(event.target.value)}
-          placeholder="Buscar prendas"
-        />
-      </div>
-    </form>
-  );
-
-  return (
-    <>
-      <header className="site-header">
-        <div className="container header-inner header-inner--compact header-inner--mobile-balanced">
-          <Link
-            to="/"
-            className={`brand-mark brand-mark--desktop ${hideBrand ? "brand-mark--hidden" : ""}`}
-            aria-label="ESADAR"
-          >
-            <img
-              src={esadarWordmark}
-              alt="ESADAR"
-              className="brand-mark__logo"
-              decoding="async"
-            />
-          </Link>
-
-          {isCheckoutView || isAccountView || isAdminView ? (
-            <span
-              className="header-search-desktop-placeholder"
-              aria-hidden="true"
-            />
-          ) : (
-            desktopSearchContent
-          )}
-
-          <div className="header-actions header-actions--ordered header-actions--desktop">
-            {isAuthenticated ? (
-              <>
-                <span className="user-greeting">
-                  Hola, {user?.firstName || ""}
-                </span>
-                {renderIconNavLink(
-                  "/cuenta/perfil",
-                  "Mi cuenta",
-                  <AccountIcon />,
-                  "",
-                  actionNotifications.account.total,
-                )}
-                {isAdmin
-                  ? renderIconNavLink(
-                      "/admin/articles",
-                      "Admin",
-                      <AdminIcon />,
-                      "",
-                      actionNotifications.admin.total,
-                    )
-                  : null}
-                {renderIconNavLink(
-                  "/cuenta/guardados",
-                  "Guardados",
-                  <HeartIcon />,
-                )}
-                {renderCartButton(desktopCartButtonRef)}
-                {renderIconButton(handleLogout, "Salir", <LogoutIcon />)}
-              </>
-            ) : (
-              <>
-                {renderIconNavLink("/login", "Ingresar", <LoginIcon />)}
-                {renderIconNavLink(
-                  "/cuenta/guardados",
-                  "Guardados",
-                  <HeartIcon />,
-                )}
-                {renderCartButton(desktopCartButtonRef)}
-              </>
-            )}
-          </div>
-
-          <div className="header-mobile-shell">
-            {renderMenuButton()}
-            <Link
-              to="/"
-              className={`brand-mark brand-mark--mobile ${hideBrand ? "brand-mark--hidden" : ""}`}
-              aria-label="ESADAR"
-            >
-              <img
-                src={esadarWordmark}
-                alt="ESADAR"
-                className="brand-mark__logo"
-                decoding="async"
-              />
-            </Link>
-            {renderCartButton(mobileCartButtonRef, "header-mobile-shell__cart")}
-          </div>
-        </div>
-      </header>
-
-      <MobileMotionMenu
-        id={mobileMenuId}
-        open={mobileMenuOpen}
-        onClose={() => setMobileMenuOpen(false)}
-        title="Menu"
-        headerContent={
-          <img
-            src={esadarWordmark}
-            alt="ESADAR"
-            className="mobile-motion-menu__brand"
-            decoding="async"
-          />
-        }
-        searchContent={mobileSearchContent}
-        filtersContent={catalogFiltersContent}
-        sortContent={catalogSortContent}
-        filtersCount={catalogFiltersCount}
-        sortActive={catalogSortActive}
-        items={mobileMenuItems}
-      />
-
-      {flyFx ? (
-        <span
-          key={flyFx.id}
-          className="cart-fly-token"
-          style={flyFx.style}
-          aria-hidden="true"
-        >
-          <span className="cart-fly-token__core" />
-        </span>
-      ) : null}
-    </>
-  );
-}
+import { Link, NavLink, useLocation, useNavigate } from "react-router-dom";
+import { useEffect, useRef, useState } from "react";
+import { motion, useReducedMotion } from "motion/react";
+import { useAuth } from "../contexts/AuthContext.jsx";
+import { useCart } from "../contexts/CartContext.jsx";
+import MobileMotionMenu from "./MobileMotionMenu.jsx";
+import { useMobileMenu } from "../contexts/MobileMenuContext.jsx";
+import esadarWordmark from "../assets/esadar-wordmark.webp";
+
+function findVisibleTargetRect(...refs) {
+  for (const ref of refs) {
+    const element = ref.current;
+    if (!element) continue;
+    const rect = element.getBoundingClientRect();
+    if (rect.width > 0 && rect.height > 0) return rect;
+  }
+  return null;
+}
+
+function HeaderIcon({ children }) {
+  return (
+    <span className="header-icon-button__icon" aria-hidden="true">
+      {children}
+    </span>
+  );
+}
+
+function AccountIcon() {
+  return (
+    <svg
+      viewBox="0 0 24 24"
+      fill="none"
+      stroke="currentColor"
+      strokeWidth="1.9"
+      strokeLinecap="round"
+      strokeLinejoin="round"
+    >
+      <path d="M20 21a8 8 0 0 0-16 0" />
+      <circle cx="12" cy="8" r="4" />
+    </svg>
+  );
+}
+
+function HeartIcon() {
+  return (
+    <svg
+      viewBox="0 0 24 24"
+      fill="none"
+      stroke="currentColor"
+      strokeWidth="1.9"
+      strokeLinecap="round"
+      strokeLinejoin="round"
+    >
+      <path d="M20.8 4.6a5.5 5.5 0 0 0-7.8 0L12 5.6l-1-1a5.5 5.5 0 1 0-7.8 7.8l1 1L12 21l7.8-7.6 1-1a5.5 5.5 0 0 0 0-7.8z" />
+    </svg>
+  );
+}
+
+function AdminIcon() {
+  return (
+    <svg
+      viewBox="0 0 24 24"
+      fill="none"
+      stroke="currentColor"
+      strokeWidth="1.9"
+      strokeLinecap="round"
+      strokeLinejoin="round"
+    >
+      <path d="M12 3l7 4v5c0 5-3.4 7.7-7 9-3.6-1.3-7-4-7-9V7l7-4z" />
+      <path d="M9.5 12.5l1.6 1.6 3.4-3.7" />
+    </svg>
+  );
+}
+
+function LoginIcon() {
+  return (
+    <svg
+      viewBox="0 0 24 24"
+      fill="none"
+      stroke="currentColor"
+      strokeWidth="1.9"
+      strokeLinecap="round"
+      strokeLinejoin="round"
+    >
+      <path d="M15 3h4a2 2 0 0 1 2 2v14a2 2 0 0 1-2 2h-4" />
+      <path d="M10 17l5-5-5-5" />
+      <path d="M15 12H3" />
+    </svg>
+  );
+}
+
+function LogoutIcon() {
+  return (
+    <svg
+      viewBox="0 0 24 24"
+      fill="none"
+      stroke="currentColor"
+      strokeWidth="1.9"
+      strokeLinecap="round"
+      strokeLinejoin="round"
+    >
+      <path d="M9 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h4" />
+      <path d="M16 17l5-5-5-5" />
+      <path d="M21 12H9" />
+    </svg>
+  );
+}
+
+function SearchIcon() {
+  return (
+    <svg
+      viewBox="0 0 24 24"
+      fill="none"
+      stroke="currentColor"
+      strokeWidth="1.9"
+      strokeLinecap="round"
+      strokeLinejoin="round"
+    >
+      <circle cx="11" cy="11" r="7" />
+      <path d="M20 20l-3.5-3.5" />
+    </svg>
+  );
+}
+
+function FilterIcon() {
+  return (
+    <svg
+      viewBox="0 0 24 24"
+      fill="none"
+      stroke="currentColor"
+      strokeWidth="1.9"
+      strokeLinecap="round"
+      strokeLinejoin="round"
+    >
+      <path d="M4 5h16" />
+      <path d="M7 12h10" />
+      <path d="M10 19h4" />
+    </svg>
+  );
+}
+
+function SortIcon() {
+  return (
+    <svg
+      viewBox="0 0 24 24"
+      fill="none"
+      stroke="currentColor"
+      strokeWidth="1.9"
+      strokeLinecap="round"
+      strokeLinejoin="round"
+    >
+      <path d="M7 4v16" />
+      <path d="M4 7l3-3 3 3" />
+      <path d="M17 20V4" />
+      <path d="M14 17l3 3 3-3" />
+    </svg>
+  );
+}
+
+function MenuGlyph() {
+  return (
+    <svg
+      viewBox="0 0 24 24"
+      aria-hidden="true"
+      className="menu-glyph"
+      fill="none"
+      stroke="currentColor"
+      strokeWidth="2"
+      strokeLinecap="square"
+      strokeLinejoin="round"
+    >
+      <path d="M5 7h14M5 12h14M5 17h14" />
+    </svg>
+  );
+}
+
+export default function Header({ hideBrand = false }) {
+  const navigate = useNavigate();
+  const location = useLocation();
+  const { user, logout, isAuthenticated } = useAuth();
+  const { cartCount, cartFx } = useCart();
+  const {
+    catalogFiltersContent,
+    catalogSortContent,
+    catalogFiltersCount,
+    catalogSortActive,
+    clearCatalogFilters,
+    clearCatalogSort,
+  } = useMobileMenu();
+  const shouldReduceMotion = useReducedMotion();
+  const [cartPulse, setCartPulse] = useState(false);
+  const [flyFx, setFlyFx] = useState(null);
+  const [mobileMenuOpen, setMobileMenuOpen] = useState(false);
+  const [mobileSearch, setMobileSearch] = useState("");
+  const desktopCartButtonRef = useRef(null);
+  const mobileCartButtonRef = useRef(null);
+  const mobileMenuId = "esadar-mobile-menu";
+
+  useEffect(() => {
+    setMobileMenuOpen(false);
+  }, [location.pathname]);
+
+  useEffect(() => {
+    if (!cartFx?.tick) return undefined;
+
+    setCartPulse(true);
+    const timeoutId = window.setTimeout(() => setCartPulse(false), 900);
+
+    const sourceRect = cartFx.sourceRect;
+    const targetRect = findVisibleTargetRect(
+      mobileCartButtonRef,
+      desktopCartButtonRef,
+    );
+
+    if (sourceRect && targetRect) {
+      const startX = sourceRect.left + sourceRect.width / 2;
+      const startY = sourceRect.top + sourceRect.height / 2;
+      const endX = targetRect.left + targetRect.width / 2;
+      const endY = targetRect.top + targetRect.height / 2;
+      const deltaX = endX - startX;
+      const deltaY = endY - startY;
+      const curveLift = Math.max(
+        48,
+        Math.min(160, Math.abs(deltaY) * 0.55 + 72),
+      );
+
+      setFlyFx({
+        id: cartFx.tick,
+        style: {
+          "--cart-fly-start-x": `${startX}px`,
+          "--cart-fly-start-y": `${startY}px`,
+          "--cart-fly-dx": `${deltaX}px`,
+          "--cart-fly-dy": `${deltaY}px`,
+          "--cart-fly-lift": `${curveLift}px`,
+        },
+      });
+
+      const flyTimer = window.setTimeout(() => setFlyFx(null), 960);
+      return () => {
+        window.clearTimeout(timeoutId);
+        window.clearTimeout(flyTimer);
+      };
+    }
+
+    return () => window.clearTimeout(timeoutId);
+  }, [cartFx?.sourceRect, cartFx?.tick]);
+
+  const isAdmin = user?.roles?.some((role) =>
+    ["SUPER_ADMIN", "ADMIN", "OPERATOR"].includes(role),
+  );
+  const canManageUsers = user?.roles?.some((role) =>
+    ["SUPER_ADMIN", "ADMIN"].includes(role),
+  );
+  const isCheckoutView = location.pathname.startsWith("/checkout");
+  const isAccountView = location.pathname.startsWith("/cuenta");
+  const isAdminView = location.pathname.startsWith("/admin");
+
+  function openCart() {
+    navigate("/checkout/resumen", {
+      state: { replayIntro: true, replayIntroReason: "cart" },
+    });
+  }
+
+  function handleMobileSearchSubmit(event) {
+    event.preventDefault();
+    const search = mobileSearch.trim();
+    const path = search
+      ? `/articles?search=${encodeURIComponent(search)}`
+      : "/articles";
+    navigate(path, {
+      state: { scrollToCatalog: true, source: "header-search" },
+    });
+    setMobileMenuOpen(false);
+  }
+
+  function renderCartButton(buttonRef, extraClassName = "") {
+    return (
+      <button
+        ref={buttonRef}
+        type="button"
+        className={[
+          "ghost-button",
+          "cart-button",
+          "cart-button--icon-only",
+          "header-icon-button",
+          cartPulse ? "cart-button--pulse" : "",
+          extraClassName,
+        ]
+          .filter(Boolean)
+          .join(" ")}
+        onClick={openCart}
+        aria-label="Carrito"
+        title="Carrito"
+      >
+        <HeaderIcon>
+          <svg
+            className="cart-button__icon"
+            viewBox="0 0 24 24"
+            fill="none"
+            stroke="currentColor"
+            strokeWidth="2"
+            strokeLinecap="round"
+            strokeLinejoin="round"
+          >
+            <circle cx="9" cy="20" r="1.6" />
+            <circle cx="18" cy="20" r="1.6" />
+            <path d="M3 4h2.2l1.9 9.2a1 1 0 0 0 1 .8h8.8a1 1 0 0 0 1-.8L20 7H7.1" />
+          </svg>
+        </HeaderIcon>
+        <span className={`badge${cartPulse ? " badge--pulse" : ""}`}>
+          {cartCount}
+        </span>
+      </button>
+    );
+  }
+
+  function renderIconNavLink(to, label, icon, extraClassName = "") {
+    return (
+      <NavLink
+        to={to}
+        className={({ isActive }) =>
+          [
+            "ghost-button",
+            "header-icon-button",
+            isActive ? "is-active" : "",
+            extraClassName,
+          ]
+            .filter(Boolean)
+            .join(" ")
+        }
+        aria-label={label}
+        title={label}
+      >
+        <HeaderIcon>{icon}</HeaderIcon>
+        <span className="sr-only">{label}</span>
+      </NavLink>
+    );
+  }
+
+  function renderIconButton(onClick, label, icon) {
+    return (
+      <button
+        type="button"
+        className="ghost-button header-icon-button"
+        onClick={onClick}
+        aria-label={label}
+        title={label}
+      >
+        <HeaderIcon>{icon}</HeaderIcon>
+        <span className="sr-only">{label}</span>
+      </button>
+    );
+  }
+
+  const accountMenuChildren = isAuthenticated
+    ? [
+        { key: "account-orders", label: "Mis ordenes", to: "/cuenta/ordenes" },
+        { key: "account-offers", label: "Ofertas", to: "/cuenta/offers" },
+        { key: "account-saved", label: "Guardados", to: "/cuenta/guardados" },
+        { key: "account-profile", label: "Perfil", to: "/cuenta/perfil" },
+        // { key: "account-alerts", label: "Alertas", to: "/cuenta/alertas" },
+      ]
+    : [
+        { key: "account-login", label: "Ingresar", to: "/login" },
+        { key: "account-register", label: "Crear cuenta", to: "/register" },
+        { key: "account-saved", label: "Guardados", to: "/cuenta/guardados" },
+      ];
+
+  function handleLogout() {
+    logout();
+    setMobileMenuOpen(false);
+    navigate("/", { replace: true });
+    window.scrollTo({ top: 0, behavior: "auto" });
+  }
+
+  const adminMenuChildren = [
+    { key: "admin-articles", label: "Articulos", to: "/admin/articles" },
+    {
+      key: "admin-article-new",
+      label: "Nuevo articulo",
+      to: "/admin/articles/new",
+    },
+    // {
+    //   key: "admin-article-bulk",
+    //   label: "Crear multiples",
+    //   to: "/admin/articles/bulk-create",
+    // },
+    { key: "admin-orders", label: "Ordenes", to: "/admin/orders" },
+    { key: "admin-offers", label: "Ofertas", to: "/admin/offers" },
+    {
+      key: "admin-contacts",
+      label: "Contactos",
+      to: "/admin/contact-messages",
+    },
+    { key: "admin-leads", label: "Leads", to: "/admin/leads" },
+    { key: "admin-wishlists", label: "Wishlists", to: "/admin/wishlists" },
+    ...(canManageUsers
+      ? [
+          { key: "admin-users", label: "Usuarios", to: "/admin/users" },
+          { key: "admin-collecting", label: "Cobros", to: "/admin/collecting" },
+        ]
+      : []),
+    { key: "admin-shipping", label: "Envios", to: "/admin/shipping" },
+    { key: "admin-statistics", label: "Estadisticas", to: "/admin/statistics" },
+    { key: "admin-audit", label: "Auditoria", to: "/admin/audit" },
+  ];
+
+  const mobileMenuItems = [
+    // { key: "home", label: "Inicio", to: "/" },
+    {
+      key: "account",
+      label: "Mi cuenta",
+      kind: "group",
+      children: accountMenuChildren,
+    },
+    ...(isAdmin
+      ? [
+          {
+            key: "admin",
+            label: "Admin",
+            kind: "group",
+            children: adminMenuChildren,
+          },
+        ]
+      : []),
+    ...(isAuthenticated
+      ? [
+          {
+            key: "logout",
+            label: "Salir",
+            kind: "button",
+            onClick: handleLogout,
+          },
+        ]
+      : []),
+  ];
+
+  function renderMenuButton() {
+    return (
+      <span className="header-mobile-menu-wrap">
+        <motion.button
+          type="button"
+          className="ghost-button header-actions-mobile__menu"
+          aria-controls={mobileMenuId}
+          aria-expanded={mobileMenuOpen}
+          aria-label={mobileMenuOpen ? "Cerrar menu" : "Abrir menu"}
+          onClick={() => setMobileMenuOpen((current) => !current)}
+          initial={false}
+          whileTap={shouldReduceMotion ? undefined : { scale: 0.95 }}
+        >
+          <motion.span
+            animate={
+              mobileMenuOpen
+                ? { rotate: -3, scale: 0.95 }
+                : { rotate: 0, scale: 1 }
+            }
+            transition={
+              shouldReduceMotion ? { duration: 0 } : { duration: 0.12 }
+            }
+          >
+            <MenuGlyph />
+          </motion.span>
+        </motion.button>
+        <span
+          className="mobile-catalog-indicators"
+          aria-hidden={
+            catalogFiltersCount > 0 || catalogSortActive ? undefined : "true"
+          }
+        >
+          {catalogFiltersCount > 0 ? (
+            <button
+              type="button"
+              className="mobile-catalog-indicator mobile-catalog-indicator--filters"
+              onClick={(event) => {
+                event.stopPropagation();
+                clearCatalogFilters?.();
+              }}
+              aria-label={`Limpiar ${catalogFiltersCount} filtros activos`}
+              title="Limpiar filtros"
+            >
+              <FilterIcon />
+              <span>{catalogFiltersCount}</span>
+            </button>
+          ) : null}
+          {catalogSortActive ? (
+            <button
+              type="button"
+              className="mobile-catalog-indicator mobile-catalog-indicator--sort"
+              onClick={(event) => {
+                event.stopPropagation();
+                clearCatalogSort?.();
+              }}
+              aria-label="Restablecer ordenamiento"
+              title="Restablecer orden"
+            >
+              <SortIcon />
+            </button>
+          ) : null}
+        </span>
+      </span>
+    );
+  }
+
+  const mobileSearchContent = (
+    <form className="mobile-menu-search" onSubmit={handleMobileSearchSubmit}>
+      <label className="sr-only" htmlFor="mobile-menu-search-input">
+        Buscar prendas
+      </label>
+      <div className="mobile-menu-search__field">
+        <SearchIcon />
+        <input
+          id="mobile-menu-search-input"
+          className="input"
+          type="search"
+          value={mobileSearch}
+          onChange={(event) => setMobileSearch(event.target.value)}
+          placeholder="Buscar por titulo, marca o categoria"
+        />
+      </div>
+      <button
+        type="submit"
+        className="button button-secondary mobile-menu-search__submit"
+      >
+        Buscar
+      </button>
+    </form>
+  );
+
+  const desktopSearchContent = (
+    <form
+      className="header-search-desktop"
+      onSubmit={handleMobileSearchSubmit}
+      role="search"
+    >
+      <label className="sr-only" htmlFor="site-header-search-input">
+        Buscar prendas
+      </label>
+      <div className="header-search-desktop__field">
+        <SearchIcon />
+        <input
+          id="site-header-search-input"
+          className="input"
+          type="search"
+          value={mobileSearch}
+          onChange={(event) => setMobileSearch(event.target.value)}
+          placeholder="Buscar prendas"
+        />
+      </div>
+    </form>
+  );
+
+  return (
+    <>
+      <header className="site-header">
+        <div className="container header-inner header-inner--compact header-inner--mobile-balanced">
+          <Link
+            to="/"
+            className={`brand-mark brand-mark--desktop ${hideBrand ? "brand-mark--hidden" : ""}`}
+            aria-label="ESADAR"
+          >
+            <img
+              src={esadarWordmark}
+              alt="ESADAR"
+              className="brand-mark__logo"
+              decoding="async"
+            />
+          </Link>
+
+          {isCheckoutView || isAccountView || isAdminView ? (
+            <span
+              className="header-search-desktop-placeholder"
+              aria-hidden="true"
+            />
+          ) : (
+            desktopSearchContent
+          )}
+
+          <div className="header-actions header-actions--ordered header-actions--desktop">
+            {isAuthenticated ? (
+              <>
+                <span className="user-greeting">
+                  Hola, {user?.firstName || ""}
+                </span>
+                {renderIconNavLink(
+                  "/cuenta/perfil",
+                  "Mi cuenta",
+                  <AccountIcon />,
+                )}
+                {isAdmin
+                  ? renderIconNavLink("/admin/articles", "Admin", <AdminIcon />)
+                  : null}
+                {renderIconNavLink(
+                  "/cuenta/guardados",
+                  "Guardados",
+                  <HeartIcon />,
+                )}
+                {renderCartButton(desktopCartButtonRef)}
+                {renderIconButton(handleLogout, "Salir", <LogoutIcon />)}
+              </>
+            ) : (
+              <>
+                {renderIconNavLink("/login", "Ingresar", <LoginIcon />)}
+                {renderIconNavLink(
+                  "/cuenta/guardados",
+                  "Guardados",
+                  <HeartIcon />,
+                )}
+                {renderCartButton(desktopCartButtonRef)}
+              </>
+            )}
+          </div>
+
+          <div className="header-mobile-shell">
+            {renderMenuButton()}
+            <Link
+              to="/"
+              className={`brand-mark brand-mark--mobile ${hideBrand ? "brand-mark--hidden" : ""}`}
+              aria-label="ESADAR"
+            >
+              <img
+                src={esadarWordmark}
+                alt="ESADAR"
+                className="brand-mark__logo"
+                decoding="async"
+              />
+            </Link>
+            {renderCartButton(mobileCartButtonRef, "header-mobile-shell__cart")}
+          </div>
+        </div>
+      </header>
+
+      <MobileMotionMenu
+        id={mobileMenuId}
+        open={mobileMenuOpen}
+        onClose={() => setMobileMenuOpen(false)}
+        title="Menu"
+        headerContent={
+          <img
+            src={esadarWordmark}
+            alt="ESADAR"
+            className="mobile-motion-menu__brand"
+            decoding="async"
+          />
+        }
+        searchContent={mobileSearchContent}
+        filtersContent={catalogFiltersContent}
+        sortContent={catalogSortContent}
+        filtersCount={catalogFiltersCount}
+        sortActive={catalogSortActive}
+        items={mobileMenuItems}
+      />
+
+      {flyFx ? (
+        <span
+          key={flyFx.id}
+          className="cart-fly-token"
+          style={flyFx.style}
+          aria-hidden="true"
+        >
+          <span className="cart-fly-token__core" />
+        </span>
+      ) : null}
+    </>
+  );
+}
--- /mnt/data/esadar_iter2/frontend/src/components/admin/AdminToolbar.jsx	2026-05-14 15:36:25.293642891 +0000
+++ /mnt/data/esadar_badges_only_work/frontend/src/components/admin/AdminToolbar.jsx	2026-05-14 15:45:36.263181911 +0000
@@ -1,11 +1,10 @@
 import { NavLink } from 'react-router-dom';
 import { useAuth } from '../../contexts/AuthContext.jsx';
-import { useActionNotifications } from '../../contexts/ActionNotificationsContext.jsx';
 
 const leftLinks = [
   { to: '/admin/articles', label: 'Articulos', end: true },
-  { to: '/admin/orders', label: 'Ordenes', noticeKey: 'orders' },
-  { to: '/admin/offers', label: 'Ofertas', noticeKey: 'offers' },
+  { to: '/admin/orders', label: 'Ordenes' },
+  { to: '/admin/offers', label: 'Ofertas' },
   { to: '/admin/contact-messages', label: 'Contactos' },
   { to: '/admin/leads', label: 'Leads' },
   { to: '/admin/wishlists', label: 'Wishlists' },
@@ -19,37 +18,24 @@
   { to: '/admin/audit', label: 'Auditoria' },
 ];
 
-function NoticeBadge({ count }) {
-  const safeCount = Number(count || 0);
-  if (!safeCount) return null;
-  return <span className="notification-badge notification-badge--tab">{safeCount > 99 ? '99+' : safeCount}</span>;
-}
-
-function AdminTab({ to, label, end, count = 0 }) {
+function AdminTab({ to, label, end }) {
   return (
     <NavLink to={to} className={({ isActive }) => (isActive ? 'admin-tab active' : 'admin-tab')} end={end}>
-      <span>{label}</span>
-      <NoticeBadge count={count} />
+      {label}
     </NavLink>
   );
 }
 
 export default function AdminToolbar() {
   const { user } = useAuth();
-  const { summary } = useActionNotifications();
   const canManageUsers = user?.roles?.some((role) => ['SUPER_ADMIN', 'ADMIN'].includes(role));
   const visibleRightLinks = rightLinks.filter((link) => !link.adminOnly || canManageUsers);
 
-  function getNoticeCount(link) {
-    if (!link.noticeKey) return 0;
-    return summary.admin?.[link.noticeKey] || 0;
-  }
-
   return (
     <div className="admin-toolbar" aria-label="Navegacion de administracion">
       <div className="admin-toolbar__group admin-toolbar__group--left">
         {leftLinks.map((link) => (
-          <AdminTab key={link.to} {...link} count={getNoticeCount(link)} />
+          <AdminTab key={link.to} {...link} />
         ))}
       </div>
       <div className="admin-toolbar__group admin-toolbar__group--right">
--- /mnt/data/esadar_iter2/frontend/src/pages/AccountPage.jsx	2026-05-14 15:36:59.118676062 +0000
+++ /mnt/data/esadar_badges_only_work/frontend/src/pages/AccountPage.jsx	2026-05-14 15:45:36.277911961 +0000
@@ -18,13 +18,11 @@
 import { useWishlist } from "../contexts/WishlistContext.jsx";
 import { useMobileMenu } from "../contexts/MobileMenuContext.jsx";
 import { useNotification } from "../contexts/NotificationContext.jsx";
-import { useActionNotifications } from "../contexts/ActionNotificationsContext.jsx";
 import { apiDownload, apiFetch } from "../lib/api.js";
 import { formatCurrency, formatDate } from "../lib/format.js";
 import { formatPaymentMethod } from "../lib/paymentMethods.js";
 import { articlePath } from "../lib/routes.js";
 import { getNextSortDirection, sortRows } from "../lib/tableSort.js";
-import { getAcceptedOfferNoticeCount, getCustomerOrderNoticeCount } from "../lib/notification-metrics.js";
 import {
   getEmailValidationMessage,
   getFriendlyErrorMessage,
@@ -145,7 +143,6 @@
   } = useLookups();
   const { notifyMobileStatus } = useMobileMenu();
   const { notifySuccess, notifyError } = useNotification();
-  const { refreshNotifications } = useActionNotifications();
 
   const requestedTab = getActiveTab(location.pathname);
   const activeTab = isAuthenticated ? requestedTab : "guardados";
@@ -241,7 +238,6 @@
         setOrders(ordersResponse.items || []);
         setOffers(offersResponse.items || []);
         setAlerts(alertsResponse.items || []);
-        void refreshNotifications();
       } catch (err) {
         if (!ignore) {
           setProfileError(err.message || "No pudimos cargar tu cuenta.");
@@ -255,7 +251,7 @@
     return () => {
       ignore = true;
     };
-  }, [isAuthenticated, refreshNotifications]);
+  }, [isAuthenticated]);
 
   const availableColors = useMemo(() => {
     const colors = new Set();
@@ -313,34 +309,6 @@
     [offers, offersSort],
   );
 
-  const accountNoticeCounts = useMemo(() => {
-    const offerCount = getAcceptedOfferNoticeCount(offers);
-    const orderCount = getCustomerOrderNoticeCount(orders);
-    return {
-      ofertas: offerCount,
-      ordenes: orderCount,
-      total: offerCount + orderCount,
-    };
-  }, [offers, orders]);
-
-  function getTabNoticeCount(tabKey) {
-    return accountNoticeCounts[tabKey] || 0;
-  }
-
-  function renderTabLabel(tab) {
-    const count = getTabNoticeCount(tab.key);
-    return (
-      <span className="tab-label-with-badge">
-        <span>{tab.label}</span>
-        {count ? (
-          <span className="notification-badge notification-badge--tab">
-            {count > 99 ? "99+" : count}
-          </span>
-        ) : null}
-      </span>
-    );
-  }
-
   const wishlistTotalPages = Math.max(
     1,
     Math.ceil(sortedWishlistItems.length / ACCOUNT_TABLE_PAGE_SIZE),
@@ -696,26 +664,11 @@
               isActive ? "admin-tab active" : "admin-tab"
             }
           >
-            {renderTabLabel(tab)}
+            {tab.label}
           </NavLink>
         ))}
       </nav>
 
-      {isAuthenticated && accountNoticeCounts.total ? (
-        <div className="account-notification-strip" role="status">
-          {accountNoticeCounts.ofertas ? (
-            <Link to="/cuenta/offers" className="account-notification-strip__item account-notification-strip__item--offer">
-              {accountNoticeCounts.ofertas} oferta{accountNoticeCounts.ofertas === 1 ? "" : "s"} aceptada{accountNoticeCounts.ofertas === 1 ? "" : "s"}
-            </Link>
-          ) : null}
-          {accountNoticeCounts.ordenes ? (
-            <Link to="/cuenta/ordenes" className="account-notification-strip__item account-notification-strip__item--order">
-              {accountNoticeCounts.ordenes} orden{accountNoticeCounts.ordenes === 1 ? "" : "es"} con cambio de estado
-            </Link>
-          ) : null}
-        </div>
-      ) : null}
-
       {activeTab === "perfil" && isAuthenticated ? (
         <section className="section-card page-stack">
           <div className="section-heading">
--- /mnt/data/esadar_iter2/frontend/src/pages/admin/AdminOffersPage.jsx	2026-05-14 15:37:33.977920722 +0000
+++ /mnt/data/esadar_badges_only_work/frontend/src/pages/admin/AdminOffersPage.jsx	2026-05-14 15:45:36.292330517 +0000
@@ -9,7 +9,6 @@
 import { BanIcon, CheckIcon, XIcon } from "../../components/ActionIcons.jsx";
 import { useLookups } from "../../contexts/LookupsContext.jsx";
 import { useNotification } from "../../contexts/NotificationContext.jsx";
-import { useActionNotifications } from "../../contexts/ActionNotificationsContext.jsx";
 import { apiFetch } from "../../lib/api.js";
 import { formatCurrency, formatDate } from "../../lib/format.js";
 import { buildQueryString } from "../../lib/query.js";
@@ -40,7 +39,6 @@
 export default function AdminOffersPage() {
   const { categoryOptions, brandOptions } = useLookups();
   const { notifySuccess, notifyError } = useNotification();
-  const { refreshNotifications } = useActionNotifications();
   const [draftFilters, setDraftFilters] = useState(initialFilters);
   const [filters, setFilters] = useState(initialFilters);
   const [offers, setOffers] = useState([]);
@@ -137,7 +135,6 @@
       });
       notifySuccess("La oferta fue actualizada correctamente.");
       setRefreshNonce((current) => current + 1);
-      void refreshNotifications();
     } catch (err) {
       const errorMessage = err.message || "No se pudo actualizar la oferta";
       setError(errorMessage);
--- /mnt/data/esadar_iter2/frontend/src/pages/admin/AdminOrdersPage.jsx	2026-05-14 15:37:33.978569752 +0000
+++ /mnt/data/esadar_badges_only_work/frontend/src/pages/admin/AdminOrdersPage.jsx	2026-05-14 15:45:36.307348944 +0000
@@ -13,7 +13,6 @@
 } from "../../components/ActionIcons.jsx";
 import { useLookups } from "../../contexts/LookupsContext.jsx";
 import { useNotification } from "../../contexts/NotificationContext.jsx";
-import { useActionNotifications } from "../../contexts/ActionNotificationsContext.jsx";
 import { apiDownload, apiFetch } from "../../lib/api.js";
 import { formatCurrency, formatDate } from "../../lib/format.js";
 import { formatPaymentMethod } from "../../lib/paymentMethods.js";
@@ -46,7 +45,6 @@
 export default function AdminOrdersPage() {
   const { categoryOptions, brandOptions } = useLookups();
   const { notifyError, notifySuccess } = useNotification();
-  const { refreshNotifications } = useActionNotifications();
   const [draftFilters, setDraftFilters] = useState(initialFilters);
   const [filters, setFilters] = useState(initialFilters);
   const [orders, setOrders] = useState([]);
@@ -152,7 +150,6 @@
       });
       notifySuccess("La orden fue cancelada.");
       setRefreshNonce((current) => current + 1);
-      void refreshNotifications();
     } catch (err) {
       const errorMessage = err.message || "No se pudo cancelar la orden";
       setError(errorMessage);
--- /mnt/data/esadar_iter2/frontend/src/pages/admin/AdminOrderDetailPage.jsx	2026-05-14 15:37:33.979398955 +0000
+++ /mnt/data/esadar_badges_only_work/frontend/src/pages/admin/AdminOrderDetailPage.jsx	2026-05-14 15:45:36.323202846 +0000
@@ -6,7 +6,6 @@
 import SmartImage from '../../components/SmartImage.jsx';
 import { DownloadIcon } from '../../components/ActionIcons.jsx';
 import { useNotification } from '../../contexts/NotificationContext.jsx';
-import { useActionNotifications } from '../../contexts/ActionNotificationsContext.jsx';
 import { apiDownload, apiFetch } from '../../lib/api.js';
 import { formatCurrency, formatDate } from '../../lib/format.js';
 import { formatPaymentMethod } from '../../lib/paymentMethods.js';
@@ -33,7 +32,6 @@
 export default function AdminOrderDetailPage() {
   const { id } = useParams();
   const { notifySuccess, notifyError } = useNotification();
-  const { refreshNotifications } = useActionNotifications();
   const [order, setOrder] = useState(null);
   const [loading, setLoading] = useState(true);
   const [error, setError] = useState('');
@@ -84,7 +82,6 @@
 
       const response = await apiFetch(path, options);
       setOrder(response.order);
-      void refreshNotifications();
       const successMessage = 'La orden fue actualizada correctamente.';
       setMessage(successMessage);
       notifySuccess(successMessage);
--- /mnt/data/esadar_iter2/frontend/src/index.css	2026-05-14 15:38:03.692358548 +0000
+++ /mnt/data/esadar_badges_only_work/frontend/src/index.css	2026-05-14 15:45:37.962306891 +0000
@@ -14587,105 +14587,7 @@
   }
 }
 
-/* === ESADAR contextual notification badges === */
-.notification-badge {
-  position: absolute;
-  top: 3px;
-  right: 3px;
-  min-width: 18px;
-  height: 18px;
-  padding: 0 5px;
-  display: inline-grid;
-  place-items: center;
-  border: 2px solid #fff;
-  border-radius: 999px;
-  background: var(--orange, #fc4c02);
-  color: #fff;
-  font-size: 0.68rem;
-  font-weight: 900;
-  line-height: 1;
-  letter-spacing: -0.02em;
-  box-shadow: 0 8px 22px rgba(252, 76, 2, 0.35);
-  pointer-events: none;
-}
-
-.header-icon-button {
-  position: relative;
-}
-
-.notification-badge--mobile-menu {
-  top: -5px;
-  right: -5px;
-  z-index: 2;
-}
-
-.notification-badge--tab,
-.notification-badge--menu {
-  position: static;
-  min-width: 20px;
-  height: 20px;
-  border-width: 0;
-  box-shadow: none;
-  flex: 0 0 auto;
-}
-
-.notification-badge--menu {
-  margin-left: auto;
-}
-
-.menu-label-with-badge,
-.tab-label-with-badge {
-  display: inline-flex;
-  align-items: center;
-  gap: 8px;
-  min-width: 0;
-}
-
-.menu-label-with-badge {
-  width: 100%;
-  justify-content: space-between;
-}
-
-.admin-tab {
-  display: inline-flex;
-  align-items: center;
-  gap: 8px;
-}
-
-.account-notification-strip {
-  display: flex;
-  flex-wrap: wrap;
-  gap: 8px;
-  align-items: center;
-  margin-top: -8px;
-}
-
-.account-notification-strip__item {
-  display: inline-flex;
-  align-items: center;
-  min-height: 32px;
-  padding: 7px 10px;
-  border: 1px solid transparent;
-  border-radius: 0;
-  font-size: 0.82rem;
-  font-weight: 850;
-  text-decoration: none;
-  text-transform: uppercase;
-  letter-spacing: 0.04em;
-}
-
-.account-notification-strip__item--offer {
-  border-color: color-mix(in srgb, var(--orange, #fc4c02) 45%, transparent);
-  background: color-mix(in srgb, var(--orange, #fc4c02) 14%, #fff);
-  color: var(--orange, #fc4c02);
-}
-
-.account-notification-strip__item--order {
-  border-color: color-mix(in srgb, var(--aqua, #008e97) 45%, transparent);
-  background: color-mix(in srgb, var(--aqua, #008e97) 12%, #fff);
-  color: var(--aqua, #008e97);
-}
-
+/* === ESADAR article/cart/offer state badges === */
 .article-gallery-notices {
   position: absolute;
   left: 12px;
@@ -14760,21 +14662,6 @@
 }
 
 @media (max-width: 720px) {
-  .notification-badge {
-    min-width: 17px;
-    height: 17px;
-    font-size: 0.64rem;
-  }
-
-  .account-notification-strip {
-    margin-top: 0;
-  }
-
-  .account-notification-strip__item {
-    width: 100%;
-    justify-content: center;
-  }
-
   .article-gallery-notices {
     left: 8px;
     right: 8px;
--- /mnt/data/esadar_iter2/frontend/src/contexts/ActionNotificationsContext.jsx	2026-05-14 15:35:13.140676985 +0000
+++ /dev/null	2026-05-14 14:18:22.304004333 +0000
@@ -1,126 +0,0 @@
-import { createContext, useCallback, useContext, useEffect, useMemo, useState } from "react";
-import { useAuth } from "./AuthContext.jsx";
-import { apiFetch } from "../lib/api.js";
-import {
-  clampNoticeCount,
-  getAcceptedOfferNoticeCount,
-  getCountFromPagination,
-  getCustomerOrderNoticeCount,
-} from "../lib/notification-metrics.js";
-
-const emptySummary = {
-  account: { offers: 0, orders: 0, total: 0 },
-  admin: { offers: 0, orders: 0, total: 0 },
-  total: 0,
-};
-
-const ActionNotificationsContext = createContext({
-  summary: emptySummary,
-  loading: false,
-  refreshNotifications: () => Promise.resolve(emptySummary),
-});
-
-function hasAdminAccess(user) {
-  return user?.roles?.some((role) =>
-    ["SUPER_ADMIN", "ADMIN", "OPERATOR"].includes(role),
-  );
-}
-
-function buildSummary({ accountOffers = 0, accountOrders = 0, adminOffers = 0, adminOrders = 0 }) {
-  const account = {
-    offers: clampNoticeCount(accountOffers),
-    orders: clampNoticeCount(accountOrders),
-  };
-  account.total = account.offers + account.orders;
-
-  const admin = {
-    offers: clampNoticeCount(adminOffers),
-    orders: clampNoticeCount(adminOrders),
-  };
-  admin.total = admin.offers + admin.orders;
-
-  return {
-    account,
-    admin,
-    total: account.total + admin.total,
-  };
-}
-
-export function ActionNotificationsProvider({ children }) {
-  const { isAuthenticated, user, loading: authLoading } = useAuth();
-  const [summary, setSummary] = useState(emptySummary);
-  const [loading, setLoading] = useState(false);
-  const isAdmin = hasAdminAccess(user);
-
-  const refreshNotifications = useCallback(async () => {
-    if (!isAuthenticated || !user?.id) {
-      setSummary(emptySummary);
-      return emptySummary;
-    }
-
-    setLoading(true);
-    try {
-      const accountRequests = [
-        apiFetch("/api/public/offers/mine"),
-        apiFetch("/api/public/account/orders"),
-      ];
-
-      const adminRequests = isAdmin
-        ? [
-            apiFetch("/api/admin/offers?status=PENDING&page=1&pageSize=1"),
-            apiFetch("/api/admin/orders?status=RESERVED&page=1&pageSize=1"),
-            apiFetch("/api/admin/orders?status=PENDING&page=1&pageSize=1"),
-          ]
-        : [];
-
-      const [offersResult, ordersResult, adminOffersResult, reservedOrdersResult, pendingOrdersResult] =
-        await Promise.allSettled([...accountRequests, ...adminRequests]);
-
-      const offersResponse = offersResult.status === "fulfilled" ? offersResult.value : null;
-      const ordersResponse = ordersResult.status === "fulfilled" ? ordersResult.value : null;
-      const adminOffersResponse =
-        adminOffersResult?.status === "fulfilled" ? adminOffersResult.value : null;
-      const reservedOrdersResponse =
-        reservedOrdersResult?.status === "fulfilled" ? reservedOrdersResult.value : null;
-      const pendingOrdersResponse =
-        pendingOrdersResult?.status === "fulfilled" ? pendingOrdersResult.value : null;
-
-      const nextSummary = buildSummary({
-        accountOffers: getAcceptedOfferNoticeCount(offersResponse?.items || []),
-        accountOrders: getCustomerOrderNoticeCount(ordersResponse?.items || []),
-        adminOffers: getCountFromPagination(adminOffersResponse),
-        adminOrders:
-          getCountFromPagination(reservedOrdersResponse) +
-          getCountFromPagination(pendingOrdersResponse),
-      });
-
-      setSummary(nextSummary);
-      return nextSummary;
-    } catch {
-      setSummary(emptySummary);
-      return emptySummary;
-    } finally {
-      setLoading(false);
-    }
-  }, [isAdmin, isAuthenticated, user?.id]);
-
-  useEffect(() => {
-    if (authLoading) return;
-    void refreshNotifications();
-  }, [authLoading, refreshNotifications]);
-
-  const value = useMemo(
-    () => ({ summary, loading, refreshNotifications }),
-    [loading, refreshNotifications, summary],
-  );
-
-  return (
-    <ActionNotificationsContext.Provider value={value}>
-      {children}
-    </ActionNotificationsContext.Provider>
-  );
-}
-
-export function useActionNotifications() {
-  return useContext(ActionNotificationsContext);
-}
--- /mnt/data/esadar_iter2/frontend/src/lib/notification-metrics.js	2026-05-14 15:35:13.126938232 +0000
+++ /dev/null	2026-05-14 14:18:22.304004333 +0000
@@ -1,43 +0,0 @@
-export const CUSTOMER_ORDER_NOTICE_STATUSES = new Set([
-  "APPROVED",
-  "SHIPPED",
-  "CANCELLED",
-  "EXPIRED",
-]);
-
-export const ADMIN_ORDER_NOTICE_STATUSES = new Set(["RESERVED", "PENDING"]);
-
-export function getOfferDisplayStatus(offer) {
-  if (offer?.consumedAt || offer?.status === "USED") return "USED";
-  return String(offer?.status || "PENDING").toUpperCase();
-}
-
-export function getAcceptedOfferNoticeCount(offers = []) {
-  return (offers || []).filter(
-    (offer) => getOfferDisplayStatus(offer) === "ACCEPTED" && !offer?.consumedAt,
-  ).length;
-}
-
-export function getCustomerOrderNoticeCount(orders = []) {
-  return (orders || []).filter((order) =>
-    CUSTOMER_ORDER_NOTICE_STATUSES.has(String(order?.orderStatus || "").toUpperCase()),
-  ).length;
-}
-
-export function getAdminOrderNoticeCount(orders = []) {
-  return (orders || []).filter((order) =>
-    ADMIN_ORDER_NOTICE_STATUSES.has(String(order?.orderStatus || "").toUpperCase()),
-  ).length;
-}
-
-export function getCountFromPagination(response) {
-  if (!response) return 0;
-  const total = Number(response.pagination?.total ?? response.total ?? 0);
-  if (Number.isFinite(total)) return total;
-  return Array.isArray(response.items) ? response.items.length : 0;
-}
-
-export function clampNoticeCount(value) {
-  const count = Math.max(0, Number(value || 0));
-  return Number.isFinite(count) ? count : 0;
-}
